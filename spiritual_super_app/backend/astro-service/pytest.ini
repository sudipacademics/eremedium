[pytest]
testpaths = tests
# Rooted at the service directory so `from app...` imports resolve without an installed package.
pythonpath = .
addopts = -q --strict-markers
filterwarnings =
    error::DeprecationWarning
