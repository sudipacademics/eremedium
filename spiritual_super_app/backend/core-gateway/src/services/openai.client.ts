/** @deprecated Import from `./llm.client.js` — kept so existing imports keep compiling. */
export {
  LlmClient as OpenAiClient,
  LlmError as OpenAiError,
  LlmClient,
  LlmError,
  type ChatMessage,
  type ChatCompletionResult,
} from './llm.client.js';
