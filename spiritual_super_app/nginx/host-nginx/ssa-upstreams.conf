# Shared SSA upstreams + maps. Loaded by zz-ssa-upstreams.conf so both
# zz-astro.e-remedium.in and zz-vedsutra.in can proxy without redeclaring them.

upstream ssa_gateway {
    server 127.0.0.1:8000 max_fails=3 fail_timeout=10s;
    keepalive 32;
}

upstream ssa_astro {
    server 127.0.0.1:8001 max_fails=3 fail_timeout=10s;
    keepalive 16;
}

upstream ssa_livekit {
    server 127.0.0.1:7880 max_fails=3 fail_timeout=10s;
}

upstream ssa_web {
    server 127.0.0.1:3100 max_fails=3 fail_timeout=10s;
    keepalive 16;
}

map $http_upgrade $ssa_connection_upgrade {
    default upgrade;
    ''      close;
}

map "$ssa_ip_allowed$ssa_gate_header_ok" $ssa_api_allowed {
    default 1;
    "00"    0;
}
